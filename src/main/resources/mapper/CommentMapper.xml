<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="icu.shishc.mapper.CommentMapper">
    <resultMap id="BaseResultMap" type="icu.shishc.entity.Comment">
        <result column="comment_id" property="commentId"/>
        <result column="blog_id" property="blogId"/>
        <result column="username" property="username"/>
        <result column="email" property="email"/>
        <result column="content" property="content"/>
        <result column="gmt_create" property="gmtCreate"/>
        <result column="parent_comment_id" property="parentCommentId"/>
    </resultMap>
    <insert id="saveComment"></insert>
    <select id="findByParentIdNull" resultType="icu.shishc.entity.Comment"></select>
    <select id="findByParentIdNotNull" resultType="icu.shishc.entity.Comment"></select>
    <select id="findByReplyId" resultType="icu.shishc.entity.Comment"></select>

</mapper>
